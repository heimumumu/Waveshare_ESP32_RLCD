# 账号地址更新

项目账号已更名为 `heimumumu`，仓库为 `https://github.com/heimumumu/Waveshare_ESP32_RLCD`。

本轮更新 Windows / macOS 界面、开发板界面、模拟器和说明文档中的地址，并重新构建 Windows Reporter、固件与安装器。公开联系 QQ 保留。

此次发布标签为 `v1.0.0-rc.2`，程序内部版本仍为 1.0.0。此前实机验收记录仅对应其记录的旧产物哈希；本轮新产物的自检及清理验证见 RELEASE_CANDIDATE.json 和 ACCOUNT_UPDATE.json，尚未重新安装或刷写验收。macOS 界面源码已更新，未生成 Mac 安装包。

为清理提交作者中的旧账号信息，仓库将以当前源码重新建立根提交；已有克隆建议重新克隆，避免把旧历史合并回来。旧发布附件和标签将撤下。已经下载的文件、第三方缓存及 GitHub 保留的历史对象不能由这次更新保证消除。
